version: '1.0b1'
data:
  # Data release information
  UDF-10:
    datacube: '{data_dir}/{data_version}/DATACUBE_UDF-10.fits'
    expcube: '{data_dir}/{data_version}/EXPMAP_UDF-10.fits'
    expima: '{data_dir}/{data_version}/EXPMAP-IMAGE_UDF-10.fits'
    white: '{data_dir}/{data_version}/IMAGE_UDF-10.fits'
  UDF-MOSAIC:
    datacube: '{data_dir}/{data_version}/DATACUBE_UDF-MOSAIC.fits'
    expcube: '{data_dir}/{data_version}/EXPMAP_UDF-MOSAIC.fits'
    expima: '{data_dir}/{data_version}/EXPMAP-IMAGE_UDF-MOSAIC.fits'
    white: '{data_dir}/{data_version}/IMAGE_UDF-MOSAIC.fits'

  # NoiseChisel segmentation map and catalog
  # ncseg: /muse/UDF/private/analysis/NoiseChisel/NewSeg_2.fits
  # nccat: /muse/UDF/private/analysis/NoiseChisel/Output_NewSeg_2.fits

  # MUSE catalogs
  # mastercat: /muse/UDF/private/catalogs/master/master_idlist.vot
  # colid: ID
  # colz: UDF10_Z
  # coltype: UDF10_TYPE
  # colactive: ACTIVE
  # colhasz: UDF10_HASZ
  # colconfid: UDF10_CONFID

  # name of sky spectra fits file
  sky: /muse/UDF/private/analysis/UDF-10/refdata/sky-ref-spectra.fits
