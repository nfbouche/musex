[tox]
envlist = py36

[testenv]
whitelist_externals =
    which
deps=
    wheel
    pytest
    pytest-cov
commands=
    python --version
    which python
    which pip
    which pytest
    pip freeze -l
    pytest {posargs} --cov musex --cov-report term tests
